# Platform database connection
LOG_DB_HOST=localhost
LOG_DB_PORT=5432
LOG_DB_USER=log_observability
LOG_DB_PASSWORD=change-me
LOG_DB_NAME=log_observability

# API runtime
APP_PORT=8810

# Optional image settings for docker-compose.yml
IMAGE_REPOSITORY=log-observability-api
IMAGE_TAG=latest
