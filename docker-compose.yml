services:
  log-observability-api:
    image: ${IMAGE_REPOSITORY:-log-observability-api}:${IMAGE_TAG:-latest}
    container_name: log-observability-api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${APP_PORT:-8810}:8810"
